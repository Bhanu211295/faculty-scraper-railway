# LLM providers - pick one (or all three)
google-genai>=0.3.0      # Gemini (free tier, default)
groq>=0.11.0              # Groq (free tier, fast)
anthropic>=0.40.0         # Anthropic Claude (paid, best quality)

# Web scraping and parsing
beautifulsoup4>=4.12.0
requests>=2.31.0
pandas>=2.0.0

# Browser automation (for advanced features: scrolling, pagination, clicking)
playwright>=1.45.0

# Web frameworks
flask>=3.0.0
streamlit>=1.28.0
